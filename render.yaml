services:
  - type: web
    name: match-streams
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install --with-deps
    startCommand: python main.py
    autoDeploy: true
